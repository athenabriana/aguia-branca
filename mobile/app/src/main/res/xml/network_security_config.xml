<?xml version="1.0" encoding="utf-8"?>
<!-- Release: somente HTTPS. Nenhum tráfego em texto puro (a variante debug libera só o emulador/localhost). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
