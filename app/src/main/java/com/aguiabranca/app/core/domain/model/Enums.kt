package com.aguiabranca.app.core.domain.model

enum class Role { OPERADOR, GESTOR, LIDER }

enum class Division { PASSAGEIROS, COMERCIO, LOGISTICA, CORPORATIVO }

enum class IdeaStatus { SUBMETIDA, EM_ANALISE, APROVADA, REJEITADA, IMPLEMENTADA }

enum class ProjectStage { PLANEJAMENTO, EM_EXECUCAO, CONCLUIDO, CANCELADO }

enum class Pillar { DIRECIONAMENTO, IDEIAS, PROJETOS, MENSURACAO }

enum class Period { THIS_MONTH, LAST_QUARTER, THIS_YEAR, ALL }
